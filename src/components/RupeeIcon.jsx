import { LuIndianRupee } from "react-icons/lu";

/**
 * Rupee (₹) icon used everywhere the app used a dollar icon.
 * Same props as the react-icons components (size, className, ...).
 */
const RupeeIcon = (props) => <LuIndianRupee {...props} />;

export default RupeeIcon;
