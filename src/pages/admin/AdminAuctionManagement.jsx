import { useCallback, useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { FiEdit3, FiEye, FiPlus, FiRefreshCw, FiTrash2 } from "react-icons/fi";
import toast from "react-hot-toast";
import { deleteAuction, getAuctions, updateAuctionStatus } from "../../api/auctionApi";
import AuctionStatusBadge from "../../components/auction/AuctionStatusBadge";
import LoadingScreen from "../../components/LoadingScreen";
import ErrorState from "../../components/ErrorState";
import EmptyState from "../../components/EmptyState";
const statuses=["draft","upcoming","live","completed","cancelled"];
export default function AdminAuctionManagement(){const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);try{const r=await getAuctions();setRows(r.auctions||[]);setError("")}catch(e){setError(e.response?.data?.message||"Unable to load auctions.")}finally{setLoading(false)}},[]);useEffect(()=>load(),[load]);const status=async(a,s)=>{if(s===a.status)return;try{await updateAuctionStatus(a._id,s);toast.success(`Status changed to ${s}`);load()}catch(e){toast.error(e.response?.data?.message||"Unable to update status.")}};const remove=async a=>{if(!window.confirm(`Delete "${a.name}"?`))return;try{await deleteAuction(a._id);toast.success("Auction deleted");load()}catch(e){toast.error(e.response?.data?.message||"Unable to delete auction.")}};if(loading)return <LoadingScreen label="Loading auction management..."/>;return <div className="space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-bold uppercase tracking-widest text-slate-500">Admin</p><h1 className="mt-2 text-3xl font-black">Auction Management</h1><p className="mt-2 text-slate-500 dark:text-slate-400">Create, review, update and control auction status.</p></div><div className="flex gap-2"><button onClick={load} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-4 py-3 text-sm font-bold dark:border-slate-700"><FiRefreshCw/> Refresh</button><Link to="/admin/auctions/new" className="inline-flex items-center gap-2 rounded-xl bg-slate-950 px-4 py-3 text-sm font-bold text-white dark:bg-white dark:text-slate-950"><FiPlus/> Create</Link></div></div>{error?<ErrorState message={error} onRetry={load}/>:rows.length===0?<EmptyState title="No auctions" message="Create your first auction to get started."/>:<div className="overflow-hidden rounded-2xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase tracking-wide text-slate-500 dark:border-slate-800 dark:bg-slate-800"><tr><th className="px-5 py-4">Auction</th><th className="px-5 py-4">Date</th><th className="px-5 py-4">Budget</th><th className="px-5 py-4">Status</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-100 dark:divide-slate-800">{rows.map(a=><tr key={a._id} className="hover:bg-slate-50 dark:hover:bg-slate-800/50"><td className="px-5 py-4"><p className="font-black">{a.name}</p><p className="text-xs text-slate-500">{a.maxTeams} teams · {a.maxPlayersPerTeam} players/team</p></td><td className="px-5 py-4">{new Date(a.date).toLocaleString("en-IN")}</td><td className="px-5 py-4 font-bold">₹{Number(a.startingBudget||0).toLocaleString("en-IN")}</td><td className="px-5 py-4"><div className="flex items-center gap-2"><AuctionStatusBadge status={a.status}/><select value={a.status} onChange={e=>status(a,e.target.value)} className="rounded-lg border border-slate-200 bg-transparent px-2 py-1 text-xs dark:border-slate-700">{statuses.map(s=><option key={s} value={s}>{s}</option>)}</select></div></td><td className="px-5 py-4"><div className="flex justify-end gap-2"><Link title="View" to={`/auctions/${a._id}`} className="rounded-lg border border-slate-200 p-2 dark:border-slate-700"><FiEye/></Link><Link title="Edit" to={`/admin/auctions/${a._id}/edit`} className={`rounded-lg border border-slate-200 p-2 dark:border-slate-700 ${['live','completed'].includes(a.status)?'pointer-events-none opacity-40':''}`}><FiEdit3/></Link><button title="Delete" onClick={()=>remove(a)} disabled={['live','completed'].includes(a.status)} className="rounded-lg border border-red-200 p-2 text-red-600 disabled:cursor-not-allowed disabled:opacity-30 dark:border-red-900"><FiTrash2/></button></div></td></tr>)}</tbody></table></div></div>}</div>}
