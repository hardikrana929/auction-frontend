import { useEffect, useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import toast from "react-hot-toast";
import { createAuction, getAuction, updateAuction } from "../../api/auctionApi";
import AuctionForm from "../../components/auction/AuctionForm";
import LoadingScreen from "../../components/LoadingScreen";
import ErrorState from "../../components/ErrorState";
export default function AuctionFormPage({edit=false}){const {id}=useParams(),nav=useNavigate(),[initial,setInitial]=useState(null),[loading,setLoading]=useState(edit),[error,setError]=useState(""),[saving,setSaving]=useState(false);useEffect(()=>{if(!edit)return;getAuction(id).then(r=>{if(['live','completed'].includes(r.auction?.status))setError(`Cannot edit a ${r.auction.status} auction.`);else setInitial(r.auction)}).catch(e=>setError(e.response?.data?.message||"Unable to load auction.")).finally(()=>setLoading(false))},[edit,id]);if(loading)return <LoadingScreen label="Loading auction..."/>;if(error)return <ErrorState message={error} onRetry={()=>location.reload()}/>;const submit=async data=>{setSaving(true);try{if(edit){await updateAuction(id,data);toast.success("Auction updated successfully");nav(`/auctions/${id}`)}else{const r=await createAuction(data);toast.success("Auction created successfully");nav(`/auctions/${r.auction?._id||""}`)}}catch(e){toast.error(e.response?.data?.message||"Unable to save auction.")}finally{setSaving(false)}};return <div className="mx-auto max-w-4xl space-y-6"><Link to={edit?`/auctions/${id}`:"/admin/auctions"} className="text-sm font-bold text-slate-500">← Back</Link><div className="rounded-3xl border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900 sm:p-8"><div className="mb-7"><p className="text-sm font-bold uppercase tracking-widest text-slate-500">Admin auction</p><h1 className="mt-2 text-3xl font-black">{edit?"Edit auction":"Create auction"}</h1><p className="mt-2 text-sm text-slate-500 dark:text-slate-400">Use the exact fields accepted by the AuctionPro backend.</p></div><AuctionForm initialValues={initial||undefined} onSubmit={submit} submitting={saving} submitLabel={edit?"Save changes":"Create auction"}/></div></div>}
